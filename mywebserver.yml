---
 - hosts: all
   become: yes
   tasks:
   - name: to install webserver
     apt: 
      name: apache2
      when: ansible_os_family=="Debian"

   - name: to start webserver
     service:
      name: apache2
      state: started
      when: ansible_os_family=="Debian"